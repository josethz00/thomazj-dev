import { Html, Head, Main, NextScript } from 'next/document';

/**
 * Document function
 * @returns {JSX.Element}
 */
export default function Document(): JSX.Element {
	return (
		<Html>
			<Head />
			<Main />
			<NextScript />
		</Html>
	);
}
